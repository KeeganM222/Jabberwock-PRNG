# File:     Compile_Results.py
# Author:   Keegan MacDonald
# Created:  2025.01.12@18:31
# Purpose:  Compute the number of passes / failures of each of the NIST SP
#           800-22 statistical tests on the data generated by the Jabberwock
#           library test.

testCount = 100
passes = [ 0 ] * 15

for file_index in range(testCount):
    input_file = open("Results/" + str(file_index + 1) + ".txt", "r")
    lines = input_file.readlines()
    input_file.close()
    for i in range(-15, 0):
        if lines[i].endswith("PASS\n"):
            passes[abs(i) - 1] += 1

for i in range(15):
    print("Test " + str(i + 1) + ": " + str(passes[i]) + " / ", end="")
    passRate = 100 * (float(passes[i]) / float(testCount))
    print(str(testCount) + " = " + str(passRate) + "%")
